# WeActCLI - Быстрая установка

Пошаговая инструкция по установке WeActCLI на Debian/Linux.

## 🚀 Быстрый старт (автоматическая установка)

```bash
# 1. Скачайте или скопируйте файлы проекта
cd /path/to/weactcli

# 2. Запустите автоматическую установку
./install.sh

# 3. Подключите дисплей и протестируйте
weactcli -p /dev/ttyUSB0 "Hello World!"
```

## 📋 Ручная установка

### Шаг 1: Установка зависимостей

```bash
sudo apt-get update
sudo apt-get install build-essential gcc make
```

### Шаг 2: Сборка

```bash
make clean
make
```

### Шаг 3: Установка

```bash
# Системная установка (требует sudo)
sudo make install

# ИЛИ локальная установка в ~/.local/bin
make PREFIX=~/.local install
```

### Шаг 4: Настройка прав

```bash
# Добавить пользователя в группу dialout
sudo usermod -a -G dialout $USER

# ВАЖНО: Выйдите и войдите снова для применения изменений
```

### Шаг 5: Проверка

```bash
# Найти устройство
ls -l /dev/ttyUSB* /dev/ttyACM*

# Тест (замените порт на ваш)
weactcli -p /dev/ttyUSB0 --verbose "Test message"
```

## 🔍 Поиск устройства

### Автоматический поиск

```bash
# С помощью make
make detect-ports

# С помощью утилиты
weact-utils detect
```

### Ручной поиск

```bash
# Посмотреть все serial устройства
ls -l /dev/tty{USB,ACM}*

# Посмотреть USB устройства
lsusb

# Системный лог (подключите дисплей и выполните)
dmesg | grep tty
```

### Типичные порты

- `/dev/ttyUSB0` - USB-serial адаптеры (CP210x, CH340, FTDI)
- `/dev/ttyACM0` - Arduino и CDC устройства
- `/dev/ttyUSB1` - Второе USB устройство

## ⚠️ Решение проблем

### Проблема: Permission denied

```bash
# Вариант 1: Добавить в группу (рекомендуется)
sudo usermod -a -G dialout $USER
# Затем выйти и войти

# Вариант 2: Изменить права (временно)
sudo chmod 666 /dev/ttyUSB0
```

### Проблема: Команда не найдена

```bash
# Проверить установку
which weactcli

# Если не найдена, добавить в PATH
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

### Проблема: Порт не найден

```bash
# Проверить подключение
dmesg | tail -20

# Убедиться что устройство подключено
lsusb

# Попробовать другие порты
for port in /dev/ttyUSB* /dev/ttyACM*; do
    echo "Testing $port"
    weactcli -p $port --cls 2>&1 | grep -i error || echo "OK"
done
```

### Проблема: Ошибка сборки

```bash
# Проверить GCC
gcc --version

# Переустановить build tools
sudo apt-get install --reinstall build-essential

# Очистить и пересобрать
make clean
make
```

## 📦 Состав проекта

```
weactcli/
├── weactcli.c          # Основная программа
├── weact_display.c     # Библиотека управления дисплеем
├── weact_display.h     # Заголовочный файл
├── text_renderer.c     # Рендеринг текста
├── text_renderer.h     # Заголовочный файл
├── Makefile            # Сборка
├── install.sh          # Автоматическая установка
├── weact-utils.sh      # Утилиты
├── example.c           # Пример программы
├── README.md           # Полная документация
└── INSTALL.md          # Этот файл
```

## 🎯 Первые команды

### Простой вывод

```bash
weactcli -p /dev/ttyUSB0 "Hello World"
```

### С цветом

```bash
weactcli -p /dev/ttyUSB0 -c green "Status: OK"
```

### Центрированный текст

```bash
weactcli -p /dev/ttyUSB0 --center "Welcome"
```

### Скроллинг

```bash
weactcli -p /dev/ttyUSB0 -s 30:u "Scrolling text..."
```

### Из файла

```bash
weactcli -p /dev/ttyUSB0 -f message.txt
```

### Из pipe

```bash
echo "Test" | weactcli -p /dev/ttyUSB0
date | weactcli -p /dev/ttyUSB0 --center
```

### Очистка экрана

```bash
weactcli -p /dev/ttyUSB0 --cls
```

## 🛠️ Утилиты

После установки доступна утилита `weact-utils`:

```bash
# Справка
weact-utils help

# Часы
weact-utils clock

# Системный статус
weact-utils status

# Сетевая информация
weact-utils network

# Таймер
weact-utils countdown /dev/ttyUSB0 30

# Собственное сообщение
weact-utils message /dev/ttyUSB0 "Hello!" green center
```

## 📚 Дополнительно

### Компиляция примера

```bash
gcc example.c weact_display.c text_renderer.c -o example -lm
./example /dev/ttyUSB0
```

### Создание библиотеки

```bash
make lib
sudo make install-all
```

### Использование в своем проекте

```c
#include <weact_display.h>
#include <text_renderer.h>

// Ваш код...
```

```bash
gcc myprogram.c -lweact -lm -o myprogram
```

## 🔄 Обновление

```bash
# Скачать новую версию
cd /path/to/weactcli
git pull  # или скопировать новые файлы

# Переустановить
./install.sh
```

## 🗑️ Удаление

```bash
# С помощью скрипта
./install.sh uninstall

# ИЛИ вручную
sudo make uninstall

# ИЛИ для локальной установки
make PREFIX=~/.local uninstall
```

## 📞 Поддержка

Если возникли проблемы:

1. Прочитайте раздел "Решение проблем" в README.md
2. Запустите с флагом `--verbose` для диагностики
3. Проверьте системные логи: `dmesg | tail -20`
4. Убедитесь в правах доступа к порту

## ✅ Проверочный список

- [ ] Зависимости установлены (`gcc`, `make`)
- [ ] Проект успешно собран (`make`)
- [ ] Программа установлена (`weactcli --help` работает)
- [ ] Пользователь в группе `dialout`
- [ ] Вышли и вошли после добавления в группу
- [ ] Дисплей подключен к USB
- [ ] Порт определен (`ls -l /dev/ttyUSB*`)
- [ ] Тестовая команда работает
- [ ] Можно выводить текст на дисплей

---

**Версия:** 1.0  
**Дата:** Январь 2025  
**Совместимость:** Debian 11-13, Ubuntu 20.04+, Linux 4.x+
