# 🔤 Cyrillic Guide - Руководство по кириллице

## Быстрый старт / Quick Start

```bash
# Установка / Installation
tar -xzf weactcli-linux-v2.0.tar.gz
cd weactcli-linux
./install.sh

# Простой тест / Simple test
weactcli -p /dev/ttyACM0 "Привет мир"
```

## Примеры / Examples

### Базовые команды / Basic Commands

```bash
# Простой текст
weactcli -p /dev/ttyACM0 "Привет"

# Центрированный текст
weactcli -p /dev/ttyACM0 --center "Тест"

# Большой шрифт
weactcli -p /dev/ttyACM0 -z 16 "ВНИМАНИЕ"

# Цветной текст
weactcli -p /dev/ttyACM0 -c red "Ошибка"
weactcli -p /dev/ttyACM0 -c green "ОК"
```

### Системная информация / System Info

```bash
# Время
date "+%H:%M" | weactcli -p /dev/ttyACM0 -z 16 --center

# Дата
date "+%d.%m.%Y" | weactcli -p /dev/ttyACM0 --center

# Дата и время по-русски
date "+Дата: %d.%m.%Y%nВремя: %H:%M" | weactcli -p /dev/ttyACM0

# Температура процессора
temp=$(cat /sys/class/thermal/thermal_zone0/temp)
temp_c=$((temp / 1000))
weactcli -p /dev/ttyACM0 "Температура: ${temp_c}°C"
```

### Статусы / Status Messages

```bash
# Статус системы
uptime -p | sed 's/up /Работает: /' | weactcli -p /dev/ttyACM0 -z 10

# Использование памяти
free -h | awk 'NR==2 {print "Память: "$3"/"$2}' | \
weactcli -p /dev/ttyACM0

# Загрузка процессора
top -bn1 | grep "Cpu(s)" | awk '{print "ЦП: "$2}' | \
weactcli -p /dev/ttyACM0
```

### Уведомления / Notifications

```bash
# Успешное завершение
weactcli -p /dev/ttyACM0 --center -c green -z 16 "Готово!"

# Ошибка
weactcli -p /dev/ttyACM0 --center -c red -z 16 "Ошибка!"

# Предупреждение
weactcli -p /dev/ttyACM0 --center -c yellow "Внимание"

# Информация
weactcli -p /dev/ttyACM0 --center -c cyan "Загрузка..."
```

### Скроллинг / Scrolling

```bash
# Медленный скроллинг вверх
weactcli -p /dev/ttyACM0 -s 15:u "Это длинный текст который будет прокручиваться вверх на дисплее с медленной скоростью"

# Быстрый скроллинг вниз
weactcli -p /dev/ttyACM0 -s 40:d "Быстрая прокрутка вниз"

# Скроллинг лог-файла
tail -n 20 /var/log/syslog | \
weactcli -p /dev/ttyACM0 -z 8 -s 25:u
```

## Полный алфавит / Full Alphabet

### Заглавные буквы / Uppercase
```
А Б В Г Д Е Ё Ж З И Й К Л М Н О П Р С Т У Ф Х Ц Ч Ш Щ Ъ Ы Ь Э Ю Я
```

### Строчные буквы / Lowercase
```
а б в г д е ё ж з и й к л м н о п р с т у ф х ц ч ш щ ъ ы ь э ю я
```

### Тест всех букв
```bash
weactcli -p /dev/ttyACM0 -z 8 "АБВГДЕЁЖЗИЙКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯ"
weactcli -p /dev/ttyACM0 -z 8 "абвгдеёжзийклмнопрстуфхцчшщъыьэюя"
```

## Смешанный текст / Mixed Text

Можно комбинировать русский и английский:

```bash
# Статус на двух языках
weactcli -p /dev/ttyACM0 "Status: Всё работает OK"

# Технические параметры
weactcli -p /dev/ttyACM0 "CPU: 45°C | Память: 512MB"

# Сообщения
weactcli -p /dev/ttyACM0 "System ready / Система готова"
```

## Скрипты / Scripts

### Часы на русском / Russian Clock
```bash
#!/bin/bash
PORT=/dev/ttyACM0

while true; do
    date "+Время:%n%H:%M:%S%n%d.%m.%Y" | \
    weactcli -p $PORT --center -z 10
    sleep 1
done
```

### Системный монитор / System Monitor
```bash
#!/bin/bash
PORT=/dev/ttyACM0

while true; do
    CPU=$(top -bn1 | grep "Cpu(s)" | awk '{print int($2)}')
    MEM=$(free | awk 'NR==2 {print int($3/$2*100)}')
    TEMP=$(cat /sys/class/thermal/thermal_zone0/temp)
    TEMP_C=$((TEMP / 1000))
    
    echo "ЦП: ${CPU}%
Память: ${MEM}%
Темп: ${TEMP_C}°C" | weactcli -p $PORT -z 10
    
    sleep 2
done
```

### Счётчик / Counter
```bash
#!/bin/bash
PORT=/dev/ttyACM0

for i in {1..10}; do
    weactcli -p $PORT --center -z 16 "Счёт: $i"
    sleep 1
done

weactcli -p $PORT --center -c green -z 16 "Готово!"
```

## Советы / Tips

### 1. Кодировка UTF-8 / UTF-8 Encoding

Убедитесь что терминал использует UTF-8:
```bash
locale | grep UTF-8
# Должно показать: LANG=ru_RU.UTF-8 или en_US.UTF-8
```

Если нет UTF-8:
```bash
export LC_ALL=ru_RU.UTF-8
export LANG=ru_RU.UTF-8
```

### 2. Размеры шрифтов / Font Sizes

- `-z 8` - для длинного текста / for long text
- `-z 12` - по умолчанию / default
- `-z 16` - для заголовков / for headers

### 3. Центрирование / Centering

Для коротких фраз используйте `--center`:
```bash
weactcli -p /dev/ttyACM0 --center "ОК"
```

Для длинных текстов центрирование работает построчно:
```bash
weactcli -p /dev/ttyACM0 --center "Длинный текст будет автоматически перенесён на следующую строку"
```

### 4. Цвета / Colors

```bash
-c red     # Красный / Red
-c green   # Зелёный / Green
-c blue    # Синий / Blue
-c yellow  # Жёлтый / Yellow
-c cyan    # Голубой / Cyan
-c magenta # Пурпурный / Magenta
-c white   # Белый / White (по умолчанию)
```

## Проблемы / Troubleshooting

### Вместо русских букв "???"

**Причина:** Неправильная кодировка в скрипте

**Решение:**
```bash
# Сохраните скрипт в UTF-8
# В nano: добавьте в начало файла:
# -*- coding: utf-8 -*-

# Проверьте кодировку файла
file -i your_script.sh
# Должно быть: charset=utf-8
```

### Символы отображаются квадратиками

**Причина:** Шрифт не содержит кириллицу

**Решение:** Используется DejaVu Sans - он точно содержит кириллицу.
```bash
# Проверьте наличие шрифта
ls /usr/share/fonts/truetype/dejavu/DejaVuSans.ttf
```

### Текст не влезает на экран

**Решение:** Используйте меньший шрифт или скроллинг
```bash
# Меньший шрифт
weactcli -p /dev/ttyACM0 -z 8 "Длинный текст"

# Или скроллинг
weactcli -p /dev/ttyACM0 -s 20:u "Длинный текст"
```

## Ещё примеры / More Examples

### Погода / Weather
```bash
# Если у вас есть API погоды
TEMP="22°C"
CONDITION="Облачно"
weactcli -p /dev/ttyACM0 "Погода:%n$TEMP%n$CONDITION"
```

### TODO список / TODO List
```bash
TODO="1. Проверить почту
2. Позвонить клиенту
3. Обновить систему"

echo "$TODO" | weactcli -p /dev/ttyACM0 -z 8
```

### Таймер обратного отсчёта / Countdown Timer
```bash
#!/bin/bash
PORT=/dev/ttyACM0

for i in {10..1}; do
    weactcli -p $PORT --center -z 16 "Старт через: $i"
    sleep 1
done

weactcli -p $PORT --center -c green -z 16 "ПУСК!"
```

---

**Полная поддержка кириллицы!**  
**Full Cyrillic support!**

Теперь можно отображать **настоящий русский текст**!  
Now you can display **real Russian text**!

🇷🇺 ✅
